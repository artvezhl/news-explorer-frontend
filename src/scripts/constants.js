// переменные
const popupContainer = document.querySelector('.popup-container');
const headerAuthButton = document.querySelector('.header__button');

// экспорт переменных
export { popupContainer, headerAuthButton };
