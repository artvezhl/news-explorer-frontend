class BaseComponent {
  constructor(props) {
    // this._handlers = props.handlers;
  }

  // Он принимает на вход массив обработчиков событий и вызывает приватный метод
  // Этот метод добавляет обработчики конкретным элементам
  _setHandlers() {

  }
}

export default BaseComponent;
